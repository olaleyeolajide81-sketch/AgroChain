name: Continuous Integration

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  NODE_VERSION: '18'
  RUST_VERSION: '1.70'
  PYTHON_VERSION: '3.11'

jobs:
  # Smart Contracts Testing
  test-contracts:
    name: Test Smart Contracts
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install Rust
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ env.RUST_VERSION }}
        components: rustfmt, clippy
        
    - name: Cache Rust dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
          
    - name: Run formatting check
      run: |
        cd contracts
        cargo fmt --all -- --check
        
    - name: Run clippy
      run: |
        cd contracts
        cargo clippy --all-targets --all-features -- -D warnings
        
    - name: Run unit tests
      run: |
        cd contracts
        cargo test --workspace --all-features --verbose
        
    - name: Run integration tests
      run: |
        cd contracts
        cargo test --test integration --all-features --verbose

  # Backend Testing
  test-backend:
    name: Test Backend Services
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: agrochain_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
          
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: |
          backend/package-lock.json
          backend/*/package-lock.json
          
    - name: Install dependencies
      run: |
        cd backend
        npm ci
        
    - name: Run linting
      run: |
        cd backend
        npm run lint
        
    - name: Run type checking
      run: |
        cd backend
        npm run type-check
        
    - name: Run unit tests
      run: |
        cd backend
        npm run test:unit
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/agrochain_test
        REDIS_URL: redis://localhost:6379
        NODE_ENV: test
        
    - name: Run integration tests
      run: |
        cd backend
        npm run test:integration
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/agrochain_test
        REDIS_URL: redis://localhost:6379
        NODE_ENV: test

  # Frontend Testing
  test-frontend:
    name: Test Frontend Applications
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: |
          frontend/package-lock.json
          frontend/*/package-lock.json
          
    - name: Install dependencies
      run: |
        cd frontend
        npm ci
        
    - name: Run linting
      run: |
        cd frontend
        npm run lint
        
    - name: Run type checking
      run: |
        cd frontend
        npm run type-check
        
    - name: Run unit tests
      run: |
        cd frontend
        npm run test:unit
        
    - name: Run component tests
      run: |
        cd frontend
        npm run test:component
        
    - name: Build applications
      run: |
        cd frontend
        npm run build

  # IoT Testing
  test-iot:
    name: Test IoT Components
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        cache-dependency-path: iot/requirements.txt
        
    - name: Install dependencies
      run: |
        cd iot
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        
    - name: Run linting
      run: |
        cd iot
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        
    - name: Run type checking
      run: |
        cd iot
        mypy . --ignore-missing-imports
        
    - name: Run unit tests
      run: |
        cd iot
        python -m pytest tests/unit/ -v --cov=. --cov-report=xml
        
    - name: Run integration tests
      run: |
        cd iot
        python -m pytest tests/integration/ -v

  # Security Scanning
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
        
    - name: Upload Trivy scan results
      uses: github/codeql-action/upload-sarif@v2
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'
        
    - name: Run npm audit
      run: |
        cd backend && npm audit --audit-level moderate || true
        cd frontend && npm audit --audit-level moderate || true
        
    - name: Run cargo audit
      run: |
        cd contracts
        cargo install cargo-audit
        cargo audit || true

  # Docker Build Test
  docker-build:
    name: Test Docker Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      
    - name: Build API Gateway
      run: |
        cd backend/api-gateway
        docker build -t agrochain/api-gateway:test .
        
    - name: Build Farmer Dashboard
      run: |
        cd frontend/farmer-dashboard
        docker build -t agrochain/farmer-dashboard:test .
        
    - name: Build IoT Gateway
      run: |
        cd iot/gateway-software
        docker build -t agrochain/iot-gateway:test .

  # End-to-End Testing
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    needs: [test-contracts, test-backend, test-frontend, test-iot]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        
    - name: Start services
      run: |
        docker-compose -f docker-compose.test.yml up -d
        sleep 60
        
    - name: Install E2E dependencies
      run: |
        cd tests/e2e
        npm install
        
    - name: Run E2E tests
      run: |
        cd tests/e2e
        npm run test
        
    - name: Upload E2E test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: e2e-test-results
        path: tests/e2e/test-results/
        
    - name: Cleanup
      if: always()
      run: |
        docker-compose -f docker-compose.test.yml down -v
        docker system prune -f

  # Performance Testing
  performance-tests:
    name: Performance Tests
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        
    - name: Install k6
      run: |
        sudo gpg -k
        sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --import <(curl -sS https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /dev/stdout)
        echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
        sudo apt-get update
        sudo apt-get install k6
        
    - name: Start services
      run: |
        docker-compose -f docker-compose.test.yml up -d
        sleep 60
        
    - name: Run performance tests
      run: |
        cd tests/performance
        k6 run --out json=results.json load-test.js
        
    - name: Upload performance results
      uses: actions/upload-artifact@v3
      with:
        name: performance-results
        path: tests/performance/results.json
        
    - name: Cleanup
      if: always()
      run: |
        docker-compose -f docker-compose.test.yml down -v

  # Build Status
  build-status:
    name: Build Status
    runs-on: ubuntu-latest
    needs: [test-contracts, test-backend, test-frontend, test-iot, docker-build]
    if: always()
    
    steps:
    - name: Check build status
      run: |
        if [[ "${{ needs.test-contracts.result }}" == "failure" || \
              "${{ needs.test-backend.result }}" == "failure" || \
              "${{ needs.test-frontend.result }}" == "failure" || \
              "${{ needs.test-iot.result }}" == "failure" || \
              "${{ needs.docker-build.result }}" == "failure" ]]; then
          echo "Build failed"
          exit 1
        else
          echo "Build succeeded"
        fi

  # Coverage Report
  coverage-report:
    name: Coverage Report
    runs-on: ubuntu-latest
    needs: [test-contracts, test-backend, test-frontend]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        
    - name: Install Rust
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ env.RUST_VERSION }}
        
    - name: Generate coverage reports
      run: |
        # Contract coverage
        cd contracts
        cargo test --workspace --all-features --no-run --message-format=json > test-results.json
        grcov . --source-dir . --output-type lcov --output-path lcov.info --test-path test-results.json
        
        # Backend coverage
        cd ../backend
        npm run test:coverage
        
        # Frontend coverage
        cd ../frontend
        npm run test:coverage
        
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./coverage/lcov.info,./backend/coverage/lcov.info,./frontend/coverage/lcov.info
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: true
